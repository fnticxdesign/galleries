const type = new Typed(".typing", {
    strings: ["adityaa.", "xirpl1", "pacar amel", "designer"],
    typeSpeed: 100,
    backSpeed: 50,
    loop: true,
});

const typi = new Typed(".typo", {
    strings: ["my gallery", "our memories", "collections", "photos"],
    typeSpeed: 200,
    backSpeed: 100,
    loop: true,
});